{
  "name": "tileserver",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18.17.0" },
  "scripts": {
    "start": "tileserver-gl-light -p ${PORT:-8080} ${TILESET_PATH:-/data/zips.mbtiles}"
  },
  "dependencies": {
    "tileserver-gl-light": "^5.4.0"
  }
}
